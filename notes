4. Add list ops



RULES

1. DONT change lisp objects under pointers (DO it only if this object is allocated in this function and you build it)
2. If you need to change some value in context, create NEW object with modified value and BIND it instead of previous one.
